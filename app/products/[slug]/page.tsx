import {products,company} from "@/lib/site";import {notFound} from "next/navigation";export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)return notFound();const msg=encodeURIComponent("Hello Advanced Fifth Axis, I need a quotation for "+p.name);return <main><nav className="nav"><div className="wrap navin"><a className="brand" href="/">ADVANCED <span>FIFTH AXIS</span></a><a className="btn" href={"/"}>Back to Products</a></div></nav><section className="wrap productHero"><div className="productVisual"><img src={p.image} alt={p.name}/></div><div><div className="eyebrow">{p.category}</div><h1>{p.name}</h1><h3 className="arabic">{p.nameAr}</h3><p className="lead">{p.description}</p><div className="featureList">{p.features.map(f=><div key={f}>✓ {f}</div>)}</div><a className="btn quote" href={"https://wa.me/"+company.phone.replace("+","")+"?text="+msg}>Request quotation on WhatsApp</a><p className="muted small">Exact dimensions, connection, tooth configuration and specifications can be manufactured or supplied according to your project requirements.</p></div></section></main>}